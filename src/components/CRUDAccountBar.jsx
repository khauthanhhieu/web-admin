import React from 'react';
import Toolbar from '@material-ui/core/Toolbar';
import Button from '@material-ui/core/';
import AddIcon from '@material-ui/icons/Add';

export default function CRUDAccountBar() {
  return (
    <Toolbar>
      <AddIcon />
    </Toolbar>
  );
}
